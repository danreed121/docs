---
title: "cbapp-core"
description: "The shell that every ContentBase App runs inside — a standard layout, a preloaded JavaScript runtime, and declarative Stimulus controllers you wire up in HTML."
---

`cbapp-core` is the **shell** that every ContentBase App (cbapp) runs inside. It provides a standard
page layout, a JavaScript runtime that is already running before your app code does anything, and a
set of Stimulus controllers you use declaratively in HTML — loading content, calling APIs, and
showing toasts without writing any JavaScript yourself.

Your job as an app developer is to produce **server-rendered HTML view fragments** that the shell
loads into named destination elements on the page. Most interactions are wired entirely in HTML
attributes.

<CardGroup cols={2}>
  <Card title="Developer Guide" icon="book-open" href="/developer-guide">
    Start here. How the shell boots, the Stimulus foundation, meta tags, and every `cba-*` attribute
    in one place.
  </Card>
  <Card title="Controllers" icon="sliders" href="/controllers/index">
    The five controllers registered automatically when `cbapp-core.js` loads.
  </Card>
  <Card title="REST API" icon="cloud" href="/api/index">
    Content / LCMR REST services — operations, parameters, and return data.
  </Card>
  <Card title="UI Utilities" icon="bell" href="/ui/index">
    Shell-level features available to every cbapp: toasts and impact checks.
  </Card>
</CardGroup>

## The controllers at a glance

| Controller | Registered on | Purpose |
|---|---|---|
| [ViewController](/controllers/view-controller) | `<body>` | Loads HTML view fragments into destination elements |
| [RestController](/controllers/rest-controller) | `<body>` | ContentBase content operations (load, delete, restore) |
| [ApiController](/controllers/api-controller) | App root element | HTTP form submissions to client-specific endpoints |
| [DrawerController](/controllers/drawer-controller) | `<body>` | Slide-in panel with overlay and keyboard dismissal |
| [UploadController](/controllers/upload-controller) | Drop zone element | Drag-and-drop upload with type/size validation |

<Tip>
New to the shell? Read [What cbapp-core is](/developer-guide#what-cbapp-core-is) and
[How the shell boots](/developer-guide#how-the-shell-boots) before wiring up your first view.
</Tip>
